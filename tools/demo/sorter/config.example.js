// Copy to config.js (gitignored) and fill in — or pass ?token=…&url=….
window.JEVALAYA = {
  url: "http://127.0.0.1:8000",   // demo proxy (same origin) — see serve_demo.py
  token: "bench-local",           // must match the server's bearer token
};
